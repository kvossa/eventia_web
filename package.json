{
  "name": "eventia",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "pnpm -C shared build && pnpm -C backend build",
    "build:shared": "pnpm -C shared build",
    "dev:backend": "pnpm -C backend start:dev",
    "dev:frontend": "pnpm -C frontend start",
    "migrate": "pnpm -C backend migration:run",
    "seed": "pnpm -C backend seed"
  },
  "packageManager": "pnpm@10.33.4",
  "pnpm": {
    "onlyBuiltDependencies": [
      "@parcel/watcher",
      "@scarf/scarf",
      "bcrypt",
      "esbuild",
      "lmdb",
      "msgpackr-extract"
    ]
  }
}